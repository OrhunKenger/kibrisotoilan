enum SteeringType { right, left }

enum Currency { gbp, eur, usd, try_ }

enum MileageUnit { km, miles }

enum BodyType { sedan, hatchback, suv, coupe, convertible, van, pickup }

enum TransmissionType { manual, automatic, semiAutomatic }
